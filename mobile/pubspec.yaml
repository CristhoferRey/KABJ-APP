name: kabj_mobile
publish_to: 'none'
description: Estructura inicial de la app móvil.
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
